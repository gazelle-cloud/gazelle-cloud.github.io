Now I have a clear picture. Your build-graph.ps1 produces { nodes: [{id, type}], links: [{source, target, relationship}] } — compatible with
  ForceGraph2D. I need to:
  1. Create app/public/index.html with the adapted example (using type instead of group)
  2. Run the build script to generate app/public/graph.json