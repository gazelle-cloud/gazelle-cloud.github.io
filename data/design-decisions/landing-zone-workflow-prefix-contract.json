{
  "id": "landing-zone-workflow-prefix-contract",
  "mechanism": "Landing zone workflows are named lz-*.yml. The platform trigger discovers and dispatches them by prefix via GitHub API — no hardcoded list.",
  "scope": ["landing-zone"],
  "why": "Dynamic discovery removes a central registry — new landing zones participate in bulk deployment the moment their workflow merges.",
  "related": [
    { "id": "landing-zone-scaffolding-via-code-generation", "relationship": "constrains", "note": "Generated workflow must use lz-* prefix." },
    { "id": "self-contained-landing-zones", "relationship": "enables", "note": "No central registry couples landing zones." }
  ],
  "violations": [
    "Landing zone workflow not starting with lz-.",
    "Fan-out logic hardcoding workflow IDs."
  ]
}
